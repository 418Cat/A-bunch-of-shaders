// Graph's size
#define GRAPH_WIDTH 0.8
#define GRAPH_HEIGHT 0.2

// Graph's coordinates
#define GRAPH_X 0.1
#define GRAPH_Y 0.05

// Defines the time in second for a bar of height 1
#define MAX_FRAME_TIME 0.1

#define FILL_BACKGROUND true

// Opt color, `Ok` defines if the color should be accessed
struct opt_color
{
    bool Ok;
    vec3 color;
};